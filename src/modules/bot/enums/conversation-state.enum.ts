/**
 * Conversation State Enum - Bot State Machine
 *
 * Re-exports the ConversationState enum from Prisma for use in the bot module.
 * This enum defines all possible states in the WhatsApp conversation flow.
 */

export { ConversationState } from '@prisma/client';
